# Consultancy Website (fixed)

This is a cleaned React + Tailwind (v3) starter project configured for Create React App.

## How to run

1. Install dependencies:
   npm install

2. Start the development server:
   npm start

This project uses Tailwind CSS v3 to avoid Tailwind v4 PostCSS incompatibilities with CRA.
